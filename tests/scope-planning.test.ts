import test from 'node:test';import assert from 'node:assert/strict';
import {scopePlanning} from '../src/application/use-cases/scope-planning';import {createMockData} from '../src/mocks/planning';
test('payload de consulta não expõe plano, tarefa, anotação ou histórico de outra obra',()=>{const data=createMockData();const actor=data.users.find(u=>u.id==='user-2')!;actor.workIds=['obra-1'];data.plans.push({id:'secret',createdAt:'',updatedAt:'',workId:'obra-2',month:'2026-09',name:'Restrito',createdBy:'user-1'});data.planTasks.push({id:'secret-task',createdAt:'',updatedAt:'',planId:'secret',name:'Restrita',notes:'Confidencial',plannedStart:'2026-09-01',plannedEnd:'2026-09-01',progress:0,level:0,order:1});data.history.push({id:'secret-history',entityId:'deleted-secret-task',entityType:'planning',action:'plan_task_revision',authorId:'user-1',occurredAt:'',changes:{planId:'secret',reason:'Confidencial'}});const result=scopePlanning(data,actor.id);assert.ok(result.works.every(w=>w.id==='obra-1'));assert.ok(!result.plans.some(p=>p.id==='secret'));assert.ok(!result.planTasks.some(t=>t.id==='secret-task'));assert.ok(!result.history.some(h=>h.id==='secret-history'));assert.ok(result.users.every(u=>u.workIds.every(id=>id==='obra-1')));});
